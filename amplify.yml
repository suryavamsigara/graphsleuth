version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --prefix apps/web
    build:
      commands:
        - npm run build --prefix apps/web
  artifacts:
    baseDirectory: apps/web/dist
    files:
      - '**/*'
  cache:
    paths:
      - apps/web/node_modules/**/*